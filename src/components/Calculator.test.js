// Imports
import { describe, it, expect } from 'vitest';

// Tests
describe('Renders main page correctly', async () => {
  it('Should render the page correctly', async () => {
    expect(true).toBeTruthy();
  });
});

test('adds 5 + 3 to equals 8', () => {
  expect(calculator.add(5, 3).toBe(8));
});

